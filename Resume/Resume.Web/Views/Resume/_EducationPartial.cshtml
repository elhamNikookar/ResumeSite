@using Resume.Domain.ViewModels.Education
@model List<EducationViewModel> 


<!-- Experience -->
<div class="section">
    <h2 class="title title--h2"> <i class="fas fa-user-graduate mb-3"></i> تحصیلات</h2>
    <div class="timeline">

          <!-- Item -->
        @foreach (EducationViewModel item in Model)
        {
          <article class="timeline__item">
            <h5 class="title title--h3 timeline__title">@item.Title </h5>
            <span class="timeline__period">@item.StartDate — @item.EndDate</span>
            <p class="timeline__description">@item.Description </p>
        </article>
            
        }
      
    </div>
</div>